{{- if .Values.clusterRBAC.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ template "k8sgrowthbook-controller.fullname" . }}-edit
  labels:
    app.kubernetes.io/name: {{ include "k8sgrowthbook-controller.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "k8sgrowthbook-controller.chart" . }}
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
  annotations:
    {{- toYaml .Values.annotations | nindent 4 }}
rules:
- apiGroups:
  - "growthbook.infra.doodle.com"
  resources:
  - growthbookfeatures
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - "growthbook.infra.doodle.com"
  resources:
  - growthbookfeatures/status
  verbs:
  - get
{{- end }}
